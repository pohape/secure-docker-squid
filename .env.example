# Copy to .env and set REAL values there. .env is gitignored.
# DO NOT put real credentials in this example file (public repo).

# Shared proxy credentials (same on every server by design)
PROXY_USER=changeme
PROXY_PASS=changeme

# Host port to expose the proxy on
PROXY_PORT=3128
